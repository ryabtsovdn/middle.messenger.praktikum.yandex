const API_DOMAIN = 'ya-praktikum.tech';

export const API_URL = `https://${API_DOMAIN}/api/v2`;

export const RESOURCES_URL = `${API_URL}/resources`;

export const WS_URL = `wss://${API_DOMAIN}/ws/chats`;
