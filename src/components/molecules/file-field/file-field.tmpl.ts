export default `
    <label for="{{id}}" class="file-field {{className}}">
        <input id="{{id}}" class="file-field__input" type="file" name={{name}} value={{file}}>
        <span class="file-field__text">Выбрать файл на компьютере</span>
    </label>
`;
